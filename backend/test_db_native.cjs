const { MongoClient } = require('mongodb'); async function run() { const client = new MongoClient('mongodb://127.0.0.1:27017'); try { await client.connect(); const db = client.db('moratea'); const pendingOrders = await db.collection('orders').countDocuments({ status: { $in: ['Pending', 'Processing'] } }); const today = new Date(); today.setHours(0,0,0,0); const todayOrders = await db.collection('orders').countDocuments({ createdAt: { $gte: today } }); console.log('Pending orders:', pendingOrders); console.log('Today orders:', todayOrders); } finally { await client.close(); } } run().catch(console.dir);
