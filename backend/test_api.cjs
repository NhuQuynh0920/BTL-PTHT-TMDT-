const axios = require('axios'); async function test() { try { const {data: loginData} = await axios.post('http://localhost:5000/api/users/login', {email: 'linhngocho0912@gmail.com', password: 'password'}); console.log('Login:', loginData.token ? 'Success' : 'Fail'); const {data: notifData} = await axios.get('http://localhost:5000/api/notifications', {headers: {Authorization: 'Bearer ' + loginData.token}}); console.log('Notifications length:', notifData.notifications ? notifData.notifications.length : 'undefined', 'unread:', notifData.unreadCount); } catch(e) { console.error('API Error:', e.response ? e.response.data : e.message); } } test();
