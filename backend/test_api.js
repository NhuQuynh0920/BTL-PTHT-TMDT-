const axios = require('axios'); async function test() { try { const {data: loginData} = await axios.post('http://localhost:5000/api/users/auth', {email: 'admin@moratea.com', password: 'password'}); console.log('Login:', loginData.token ? 'Success' : 'Fail'); const {data: notifData} = await axios.get('http://localhost:5000/api/notifications', {headers: {Authorization: 'Bearer ' + loginData.token}}); console.log('Notifications:', JSON.stringify(notifData).substring(0, 200)); } catch(e) { console.error('API Error:', e.response ? e.response.data : e.message); } } test();
