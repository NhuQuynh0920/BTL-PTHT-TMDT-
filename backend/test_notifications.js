import mongoose from 'mongoose'; import Order from './models/Order.js'; import Review from './models/Review.js'; mongoose.connect('mongodb://127.0.0.1:27017/moratea').then(async () => { try { const pendingOrders = await Order.find({ status: { $in: ['Pending', 'Processing'] } }).sort({ createdAt: -1 }).limit(5).populate('user', 'name fullName'); console.log('Pending orders:', pendingOrders.length); const unrepliedReviews = await Review.find({ $or: [ { adminReply: '' }, { adminReply: null }, { adminReply: { $exists: false } } ] }).sort({ createdAt: -1 }).limit(5).populate('productId', 'name').populate('userId', 'name fullName'); console.log('Unreplied reviews:', unrepliedReviews.length); console.log('Success'); } catch(e) { console.error('Error:', e); } finally { mongoose.disconnect(); } });
